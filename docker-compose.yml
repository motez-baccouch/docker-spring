version: '3'

services:
  springbootapp:
    image: spring-mongo:latest
    container_name: my-springboot-application
    ports:
      - "8081:8081"

  mongodb:
    image: mongo:latest
    container_name: my-mongodatabase
    ports:
      - "28000:27017"
